name: Build IPA and APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Prepare Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/ios @capacitor/android @capacitor/assets
          npx cap init "Mala Counter" "com.mala.counter" --web-dir www
          npx cap add ios
          npx cap add android

      - name: Generate Native Icons
        run: |
          npx @capacitor/assets generate --ios --android
          npx cap copy

      - name: Force Landscape and Debug
        run: |
          # Apply iOS settings
          plutil -replace UISupportedInterfaceOrientations -xml "<array><string>UIInterfaceOrientationLandscapeLeft</string><string>UIInterfaceOrientationLandscapeRight</string></array>" ios/App/App/Info.plist
          plutil -replace UISupportedInterfaceOrientations~ipad -xml "<array><string>UIInterfaceOrientationLandscapeLeft</string><string>UIInterfaceOrientationLandscapeRight</string></array>" ios/App/App/Info.plist
          plutil -replace UIRequiresFullScreen -bool YES ios/App/App/Info.plist
          
          # Apply Android settings
          sed -i '' 's/android:name=".MainActivity"/android:name=".MainActivity" android:screenOrientation="landscape"/g' android/app/src/main/AndroidManifest.xml
          
          # DEBUGGING: Print files to logs so we can see if they actually changed
          echo "--- Checking Info.plist ---"
          cat ios/App/App/Info.plist
          echo "--- Checking AndroidManifest.xml ---"
          cat android/app/src/main/AndroidManifest.xml

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Compile iOS
        run: |
          cd ios/App
          xcodebuild -workspace App.xcworkspace -scheme App -configuration Debug -sdk iphoneos -derivedDataPath build CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""
          
      - name: Package IPA
        run: |
          mkdir -p Payload
          cp -r ios/App/build/Build/Products/Debug-iphoneos/App.app Payload/
          zip -r MalaCounter.ipa Payload
          
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Mala-Counter-Builds
          path: |
            MalaCounter.ipa
            android/app/build/outputs/apk/debug/app-debug.apk
